# Springboot 配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/yuc
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  redis:
    host: localhost
    port: 6379
server:
  port: 8089

# mybatis-plus 配置
mybatis-plus:
  mapper-locations: classpath*:/mapper/*.xml
